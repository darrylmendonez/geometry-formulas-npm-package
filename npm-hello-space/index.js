const {area, perimeter } = require('npm-hello-world-from-space')

let radius1 = 3;
let radius2 = 4;

console.log(`the area of a circle with radius ${radius1} = ${area(radius1)}.`)
console.log(`the circumference of a circle with radius ${radius2} = ${perimeter(radius2)}.`)